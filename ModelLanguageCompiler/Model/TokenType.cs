namespace ModelLanguageCompiler.Model
{
    public enum TokenType
    {
        Keyword,
        Identifier,
        Number,
        HexNumber,
        Operator,
        Delimiter,
        LogicalConstant,
        Comment,
        Error
    }
}
