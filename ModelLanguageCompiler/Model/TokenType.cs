namespace ModelLanguageCompiler.Model
{
    public enum TokenType
    {
        Keyword,
        Identifier,
        Number,
        Operator,
        Delimiter,
        LogicalConstant,
        Error
    }
}
